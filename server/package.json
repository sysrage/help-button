{
  "name": "help-server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.17.1",
    "moment": "^2.24.0",
    "pushover-notifications": "^1.2.2"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.25.2"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd ../button && yarn && yarn build && cd ../server && yarn",
    "lint": "eslint \"**/*.js\" --ignore-pattern node_modules/"
  }
}
